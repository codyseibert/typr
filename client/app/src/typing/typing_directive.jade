div
  input.type(
    autofocus
    ng-blur="blur()"
    ng-model="text"
    ng-keypress="keypress($event)"
  )

div.code(
  ng-hide="paused"
)

button(
  ng-show="paused"
  ng-click="start()"
) Start!
